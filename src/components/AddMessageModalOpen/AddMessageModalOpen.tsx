import MessageModal from '../MessageModal/MessageModal';

const AddMessageModalOpen = () => {
  return (
    <MessageModal modalKey='isAddMessageModalOpen'>
      Contact successfully <strong>added</strong>
    </MessageModal>
  );
};

export default AddMessageModalOpen;
